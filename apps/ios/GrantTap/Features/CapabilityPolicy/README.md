# Capability Policy UI

`GlobalCapabilityPolicySection.swift` renders machine-wide MCP, skill, and CLI
controls in Tools & Usage. Provider labels are availability metadata; usage
history below it contains only observed execution.

The bridge precedence rule lives in
`apps/bridge/src/features/CapabilityPolicy/CapabilityPolicy.ts`.
