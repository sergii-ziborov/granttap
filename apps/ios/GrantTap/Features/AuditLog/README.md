# Log filters

`AuditLogFilter.swift` owns the local diagnostic log's stable `All` and
`Errors` filtering semantics. The iPhone log is a concise device-local support
view; the web console remains the richer audit surface.
