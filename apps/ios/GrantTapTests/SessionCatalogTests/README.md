# Session catalog tests

`ProviderAndWireTests.swift` covers provider identity and tolerant numeric wire
decoding. `RemappingTests.swift` covers local-to-native session replacement and
field preservation. Both extend the `SessionCatalogTests` XCTest case.
